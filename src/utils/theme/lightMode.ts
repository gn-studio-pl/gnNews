import { devices, palette } from './themeConfig';

export const lightTheme = {
  colors: {
    background: palette.black,
    invertedBackground: palette.white,
    font: palette.white,
    invertedFont: palette.black,
    primary: palette.primary,
  },

  devices,
};
